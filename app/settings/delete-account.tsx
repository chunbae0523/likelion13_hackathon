import { View, Text } from 'react-native';
export default function DeleteAccount() {
  return (
    <View style={{ flex:1, alignItems:'center', justifyContent:'center' }}>
      <Text style={{ fontSize: 24, color: "pink" }}>Delete Account</Text>
    </View>
  );
}
